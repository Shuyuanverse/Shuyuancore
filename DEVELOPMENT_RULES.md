# ShuyuanCore 开发规则（完整细化版）
版本：v1.0 | 生效日期：2026-05-26
适用范围：所有 AI Agent（Trae）及人类协作者
仓库状态：骨架已完成（空占位文件），文档已就绪
**📋 文档受众与用途**
**本文档受众**：AI 编程助手（Trae、Cursor、Codex、Gemini 等）及人类协作者。
**本文档规定了** AI 编程助手在开发 ShuyuanCore 项目时必须遵守的所有行为准则、沟通方式、代码规范及与创始人协作的流程。
产品架构层面的要求（如低耦合、配置驱动、模块边界、数据库规则）既是产品质量标准，也是 AI 编程助手编写代码时必须遵循的技术约束。
文档中的"开发流程规则"是指 AI 编程助手执行开发任务时应遵循的步骤，而非产品运行时的内部逻辑。
**⚡ 快速参考卡**
| ✅ 必须做 | 🚫 禁止做 |
|---|---|
| 开发前阅读 docs/ 规范文档 | 在未经批准的情况下创建新子目录 |
| 先输出设计提案，确认后再编码 | 未经设计探讨直接写实现代码 |
| 输出理解确认清单，创始人确认后开始 | 修改超出当前任务范围的代码 |
| 通过 interfaces.py 跨模块通信 | 跨模块直接导入具体实现 |
| 从环境变量读取敏感信息 | 硬编码 API Key / 密码 |
| 每个 commit 确保代码可运行 | 提交无法通过 smoke test 的代码 |
| 操作前快照，危险操作等审批 | 使用裸 except: |
| 清理 **pycache** 后再验证 | 修改已锁定的关键参数 |
## 一、总体定位与设计哲学
### 1.1 规范驱动开发（Spec-Driven Development）
• 项目以 docs/ 目录下的四份核心规范为事实来源：
产品方案 → 技术架构 → API接口文档 → 数据库Schema。
• 任何代码实现必须严格遵循这些规范；当规范之间出现冲突时，按以下优先级裁决：
**产品方案 > 技术架构 > API接口文档 > 数据库Schema**
**创始人明确指令 > 所有文档**。
• 禁止自行发明规范中未定义的功能或接口。
### 1.2 规则约束优先于智能推断
• 本项目的开发模式是规则约束型，AI 必须在规则边界内行动，不得擅自扩展或修改需求。
• 遇到不确定的情况，必须暂停并向创始人确认，禁止假设。
### 1.3 设计目标
• **可读性优先**：代码是写给人看的，其次才是机器。
• **低耦合高内聚**：模块通过接口通信，依赖注入，禁止循环依赖。
• **生产级质量**：代码必须经过测试、类型检查和代码审查。
• **配置驱动**：所有可变值从配置文件或环境变量读取，禁止硬编码。
• **AI 友好**：代码结构清晰、注释完整，便于 AI 理解和维护。
## 二、项目与架构规则
### 2.1 目录结构（与当前 GitHub 仓库严格一致）
• 后端源码根目录：src/（不是 app/，不是 backend/）。
• 所有业务代码必须放在 src/ 下的对应子模块中，已创建的模块包括：
core, memory, skills, tools, agents, persona, gateway, cron, models, evolution, prediction, security。
• 每个模块目录必须包含 \_\_init\_\_.py（可为空）。
• 配置文件：config/default.yaml（非空时需遵循技术架构中的配置结构）。
• 运行时数据：data/ 目录（被 .gitignore 排除，不应提交）。
• 部署脚本：deploy/（systemd, nginx, docker 等）。
• 测试代码：tests/，目录结构应与 src/ 一一对应。
• 文档：docs/ 下的四份核心文档不得随意修改，除非经过创始人确认。
• **禁止在未更新技术架构文档并获得创始人批准的情况下**，在 src/、config/、deploy/、tests/ 等核心目录下创建新的子目录。
• 临时生成的目录（如 reports/、tmp/、logs/）必须加入 .gitignore，且不得在产品规范中引用。如需永久目录，必须先更新 docs/技术架构.md 中的项目结构。
### 2.2 模块边界与低耦合规则
• **模块间不得直接导入具体实现**：必须通过接口（interfaces.py 或 abc.py）进行通信。
例如：from src.memory.interfaces import IMemory，而非 from src.memory.core\_memory import CoreMemory。
• **每个模块必须定义自己的接口**：在模块根目录下创建 interfaces.py，存放抽象基类或 Protocol。
• **依赖通过构造函数注入**：禁止在模块内部直接实例化其他模块的具体类。
• **模块可独立禁用**：通过配置开关可以禁用某个模块，不影响其他模块运行。
• **删除操作必须级联清理所有关联存储**：
例如删除对话时，必须同时从 SQLite 删除 conversations + messages，并从 ChromaDB 删除 session\_id 对应的向量记录。禁止留下孤儿数据。
### 2.3 配置规则（含关键参数锁定）
• 所有可变值从配置读取：包括但不限于 API Key、超时、阈值、路径、模型名称。
• 配置优先级：环境变量 > config/default.yaml > 代码默认值。
• **Pydantic Settings Extra 禁令**：
禁止在 .env 或 config/default.yaml 中添加 Settings 类未定义的字段。
Pydantic 默认禁止 extra 输入，任何未定义参数都会导致启动崩溃。如需新增配置项，必须先修改 Settings 类（通常位于 src/config.py）添加对应字段。
• 敏感信息只从环境变量读取：API Key、密码等不得出现在配置文件中。
• **关键参数锁定表（禁止修改）**：
|  |  |  |  |
| --- | --- | --- | --- |
| **参数** | **值** | **来源** | **说明** |
| drift\_threshold（保护层） | 0.25 | 产品方案 | 松刹车策略，禁止修改 |
| drift\_threshold（审查 Agent） | 0.15 | 产品方案 | 禁止修改 |
| enable\_proactive | False | 产品方案 | 不主动干预，仅漂移时校准 |
| 风格编码维度 | 7维度 | 产品方案 | 风格编码器输出7维基础维度 |
| 风格锚点维度 | 128维 | 产品方案 | 7维扩展至128维 |
| 决策锚点维度 | 256维 | 产品方案 | 与风格编码器绑定，DashScope 1536维→PCA降维 |
| bound_components | True | 产品方案 | 风格编码器与决策锚点必须绑定使用 |
| Embedding 模型 | text-embedding-v2, 1536维 | 技术架构 | 模型变更需新建 ChromaDB 集合 |
| 去重阈值 | 0.95（余弦相似度） | API 文档 | 向量写入前去重 |
| 自演化触发 | 7天40次同类任务 | 产品方案 | 可配置但需慎重 |
| 子代理上限 | 5 | 产品方案 | 最大并行数 |
| 多维审视视角数 | 2-5个 | 产品方案 | Agent 根据复杂度自定 |
| 人格编译输入 | 100字 ～ 100万字 | 产品方案 | 低于100字报错 |
| 语言样本数量 | 500-1000条 | 产品方案 | 按场景分类 |
### 2.4 数据库规则（SQLite + ChromaDB）
• **唯一数据库文件**：data/state.db，所有表放在一起，禁止多文件副本。
• **连接必须执行 PRAGMA**：每个新连接（包括连接池创建的）必须执行：
**// sql**PRAGMA journal\_mode = WAL;
PRAGMA foreign\_keys = ON;
PRAGMA busy\_timeout = 5000;
在 SQLAlchemy 中应使用 connect 事件钩子统一设置。
• **测试数据库隔离**：
单元测试和集成测试必须使用独立的数据库（如 :memory: 或临时文件），禁止读写生产 state.db。每个测试类/函数应自行创建和销毁数据库（使用 pytest fixture）。
• **外键约束必须开启**：FOREIGN KEY 关联的表必须设置 ON DELETE CASCADE 或 SET NULL 等合理行为。
• **时间戳统一**：所有 created\_at, updated\_at 使用 Unix 毫秒时间戳（INTEGER），UTC 时区。
• **JSON 字段**：使用 TEXT 类型存储 JSON 字符串，应用层解析。
### 2.5 数据迁移与回填规则
• **Alembic 迁移脚本规范**：
◦ 必须包含 upgrade() 和 downgrade()，支持回滚。
◦ 每个迁移脚本只做一件事（加字段就只加字段，不混着改表或加数据）。
◦ 迁移脚本命名遵循 Alembic 默认格式（{revision\_id}\_description.py），description 部分需清晰描述变更内容。禁止手动修改 revision\_id。
◦ 提交前必须测试回滚（alembic downgrade -1 然后 alembic upgrade head）。
◦ 禁止在生产环境手动 ALTER TABLE。
• **ChromaDB 回填脚本**（从 SQLite 历史数据生成向量）必须满足：
◦ 断点续传：记录最后处理的 message\_id。
◦ 批处理：每批 20 条（DashScope API 限制）。
◦ 进度日志：每 100 条打印一次。
◦ 错误重试：失败条目记录到 failed\_ids.txt，允许重新处理。
◦ 幂等执行：重复运行不会产生重复向量（写入前去重 >0.95）。
## 三、代码规范
### 3.1 Python 版本与工具链
• Python 版本：3.10 或更高。
• 代码格式化：black，行宽 100。
• 代码检查：ruff。
• 类型检查：mypy，逐步向 --strict 靠拢。
### 3.2 命名规范
• 模块/包名：小写，无下划线（core, memory）。
• 类名：CapWords（Agent, MemoryStore）。
• 函数/方法名：小写，下划线分隔（get\_conversation）。
• 常量：全大写，下划线分隔（DRIFT\_THRESHOLD）。
• 私有属性/方法：前缀单下划线（\_prepare\_context）。
• 双下划线：一般不使用。
### 3.3 格式规范
• 缩进：4 个空格，禁止 Tab。
• 行长：最大 100 字符。
• 空行：模块级函数和类之间空两行，类内方法之间空一行。
• 导入顺序：标准库 → 第三方库 → 本地模块，各组空一行。
### 3.4 文档字符串（Docstring）
• 使用 Google 风格。
• 所有公共类、公共方法、模块级函数必须包含 docstring。
• 私有方法若逻辑复杂，也应添加简要 docstring。
• 必须包含 Args, Returns, Raises 等部分（如适用）。
### 3.5 类型注解
• 所有函数参数和返回值必须标注类型。
• 变量类型：复杂类型（如 list[dict[str, Any]]）必须标注，简单类型可省略。
• 使用 Optional[T] 代替 Union[T, None]。
• 启用 from \_\_future\_\_ import annotations。
### 3.6 禁止清单（绝对不允许的行为）
1. **实验/学习代码写入项目目录**：
任何探索性、学习性代码只能写到项目外的独立目录（如 experiments/ 或 scratch/），禁止写入 src/, config/, tests/ 等核心目录。未经创始人明确指令，禁止将实验代码集成到主项目。
2. **读写路径不一致**：
同一数据的写入和读取必须使用同一个数据访问路径（同一张表、同一个 Service 方法）。禁止新建平行路径导致数据"消失"。
3. **提交未清理的 \_\_pycache\_\_ 或 .pyc 文件**：
修改 Python 源码后，必须执行 find . -name "*.pyc" -delete 清理缓存，或重启前清理 \_\_pycache\_\_。若代码改动后行为未变，首先排查缓存残留。
4. **硬编码敏感信息**：
任何 API Key、密码、Token 不得出现在代码或配置文件中，必须从环境变量读取。
5. **使用裸 except:**：
所有异常处理必须明确捕获具体异常类型。
6. **在没有接口定义的情况下跨模块直接调用**：
禁止 from src.memory.core\_memory import XXX，必须通过 interfaces.py。
7. **修改已锁定的关键参数**（见 2.3 节表格）。
8. **提交无法通过基本 smoke test 的代码**：
每个提交必须确保服务能够启动，/health 端点返回 200（或至少代码可导入）。
## 四、开发流程规则
### 4.1 开发前准备
• 阅读 docs/ 下相关的规范文档。
• 明确当前模块依赖的其他模块，确认它们已实现接口。
• 先编写模块的 interfaces.py，经创始人确认后再编写实现。
• **理解确认清单**：每个模块开发前，必须输出以下内容，创始人确认后才能开始编码：
◦ 本模块要实现的功能列表（bullet points）
◦ 依赖的其他模块及接口
◦ 关键参数和配置项（哪些会用到、从哪里读取）
◦ 可能的风险和边界条件
### 4.2 开发顺序（强依赖关系）
1. 基础设施：src/config.py、src/logging.py、src/exceptions.py
2. 模型层：src/models/（提供者、路由、主备切换）
3. 核心 Agent：src/core/agent.py、conversation.py
4. 记忆系统：L1（核心记忆）、L3（长期历史）
5. 人格编译与风格保护：src/persona/
6. 技能系统：先手动技能管理，自动提炼后置
7. 工具系统：先实现核心工具（terminal, file\_ops, web, memory, skills）
8. 多智能体协作：先实现决策+审查单链，多视角后置
9. 网关与 API：先实现 CLI 和 API 服务，平台适配器后置
10. 部署与测试：最后完善
### 4.3 提交规范
• 格式：<type>(<scope>): <subject>
• type：feat, fix, docs, style, refactor, test, chore
• scope：模块名（如 config, memory, agent）
• subject：不超过 50 字符，使用现在时
• Smoke test 要求：每个提交必须让代码库保持在可运行状态。至少保证服务可以启动（若该模块涉及服务入口）或相关导入不失败。
• **范围控制**：每个 commit 的 diff 必须仅包含当前任务相关的文件和行。提交前必须运行 git diff --stat 并确认改动范围与任务一致。如果发现需要修改超出范围的内容（如发现 bug 或需要重构），必须先暂停报告创始人，得到批准后再做。
• 禁止：提交包含未完成、无法编译或无法通过基本烟雾测试的代码。
### 4.4 测试要求
• 每个功能模块必须有对应的单元测试，放在 tests/ 下，路径与 src/ 对应。
• 测试框架：pytest + pytest-asyncio。
• 覆盖率目标：核心模块（src/config.py, src/models/, src/core/agent.py, src/memory/）行覆盖率 ≥ 85%。
• 测试文件命名：test\_<module>.py。
• 测试函数命名：test\_<function>\_<scenario>。
• 异步测试：使用 @pytest.mark.asyncio。
• 测试数据库隔离：禁止测试读写生产 state.db，必须使用 :memory: 或临时文件。
### 4.5 安全与审批规则
• 危险操作（如执行系统命令、删除文件、数据库修改）必须标记 dangerous=True。
• 危险操作必须等待用户 /approve 后才能执行，审批超时 5 分钟自动拒绝。
• **操作前快照**：所有 AI 自主执行的危险操作必须在审计日志中记录操作前的状态、操作内容和操作后的结果。删除类操作执行前必须先备份受影响数据（如删除对话前备份关联向量记录）。
• **备份策略**：
◦ 不设置自动定时备份。备份仅在以下情况执行：
▪ 创始人明确要求 AI 编程助手进行备份。
▪ 执行危险操作（如删除对话、数据库迁移、回填 ChromaDB）前，AI 编程助手必须提醒创始人确认是否需要备份，由创始人决定。
◦ 备份文件统一放在 data/backups/ 目录，命名格式 backup\_YYYYMMDD\_HHMMSS.tar.gz。
◦ 提供恢复命令 shuyuancore restore <备份文件>，恢复前将当前状态移动到 data/backups/pre\_restore/ 作为快照。
◦ 备份内容：data/state.db、data/chroma/。data/logs/ 和 data/memories/ 不备份（日志可重建，记忆文件建议用 Git 管理）。
• 审计日志的权威存储为 SQLite 表 audit\_logs（结构化、可查询）。文件日志 data/logs/audit.log 仅作为运维兜底和开发调试，两者并行但不互相替代。
• 日志中不得输出 API Key、密码等敏感信息。
• 所有工具调用和关键操作必须写入审计日志（通过 src/security/audit.py）。
### 4.7 代码变更前必须同步

- 在每次进行代码修改、新增文件、删除文件或任何会改变仓库内容的操作之前，必须确保本地工作区与 GitHub 远程仓库的 `main` 分支完全同步。
- 同步操作为：`git pull origin main`（如有冲突必须先解决）。
- 提交代码后，必须立即 `git push origin main`，确保远程仓库实时更新。
- 禁止在未同步的情况下进行任何变更，禁止在本地积累多个 commit 不推送。

### 4.8 执行指令前必须阅读规则

- 每次收到创始人发出的新指令（包括开发、修复、文档更新等），必须首先阅读 `DEVELOPMENT_RULES.md` 全文（或至少相关章节），并确认已理解当前规则版本。
- 如果规则文件在本地不存在或可能已过期，应先从 GitHub 拉取最新版本。
- 在回复创始人"开始执行"之前，必须明确声明"已阅读规则 vX.X"。
## 五、AI 协作规则（专门给 Trae）
### 5.1 会话启动规则
• 每次开发会话开始时，必须重新加载 DEVELOPMENT\_RULES.md 全文，并确认"已理解当前规则版本"。
• 每个模块开发前，必须引用相关规则条款（例如"依据 2.3 节配置规则，我将从环境变量读取 API Key"）。
### 5.2 设计探讨流程（强制前置）
**Trae 禁止在未经设计探讨的情况下直接编写实现代码。** 每个模块的开发必须按以下流程进行：
**第一步：设计提案**
Trae 必须先输出一份设计提案，包含：
• **功能拆解**：本模块要实现哪些功能，每个功能的输入/输出是什么
• **架构设计**：类/接口怎么定义，模块间怎么通信，数据怎么流转
• **技术选型**：用什么库/方法实现，为什么选这个方案
• **实现步骤**：按什么顺序写代码，每步产出什么
• **风险点**：可能踩的坑、边界条件、需要创始人决策的问题
**第二步：创始人确认**
• 创始人对设计提案进行审查，可以：批准 / 要求修改 / 提出替代方案
• Trae 不得跳过此步骤，不得在创始人确认前写任何实现代码
• 如果创始人提出修改意见，Trae 必须先修改设计提案，再次确认后才能开始编码
• **确认超时**：若创始人在 24 小时内未对设计提案作出回应，Trae 应主动提醒一次。若仍无回复，暂停任务并记录等待状态，不得擅自跳过确认。
**第三步：理解确认清单**
创始人批准设计后，Trae 输出理解确认清单（见 4.1 节），创始人最终确认。
**第四步：编码实现**
确认完成后才能开始编码。编码过程中如发现设计与实际情况不符，必须暂停并报告创始人，不得自行调整。
**核心理念：先聊清楚怎么设计，确认思路对了再动手——就像创始人当初开发探讨一样。**
### 5.3 沟通规范
• 每完成一个模块后，主动报告完成状态，格式：
✅ 已完成：<模块名>
提交哈希：<hash>
覆盖功能：<功能列表>
待确认事项：<如有>
• 不确定时暂停，向创始人确认，禁止擅自决策。
• 解释技术问题使用通俗语言，必要时附上类比。
• 首次介绍复杂概念时，提供简单例子。
• 提供选项（A/B/C）而非开放式提问。
• 多步骤任务先拆分步骤并请创始人确认顺序。
• 使用正面、积极的表达方式，不使用负面情绪词汇。
• 使用 emoji 或 Markdown 增强可读性。
• 禁止输出未经过滤的 LLM 原始结果或调试堆栈。
• 修改代码前必须先读取最新内容，禁止基于过时上下文修改。
### 5.4 安全准则
• 绝不执行未经创始人确认的危险操作。
• 不主动建议可能涉及用户数据隐私的改动。
• 不确定时停止并请求指示。
• 所有 API Key 等敏感信息提醒通过环境变量配置，禁止写入代码。
• 所有工具调用必须在审计日志中留有记录。
• **数据隐私提醒**：使用云端 AI 服务（如 DashScope, DeepSeek）时，数据传输和存储可能涉及隐私。建议：
◦ 敏感项目使用本地模型（Ollama）或企业版 API
◦ 在 .env 中配置 DISABLE\_TELEMETRY=true（如果支持）
◦ 定期清理 data/logs/ 中的敏感信息
### 5.5 代码质量标准
• 所有函数包含类型注解和 docstring。
• 所有异常明确捕获，禁止裸 except:。
• 异步函数中的所有 I/O 操作必须使用异步库（aiohttp 替代 requests，aiosqlite 替代 sqlite3）。
• 所有外部调用（LLM API、数据库）必须包含超时和重试机制（可配置，默认 30 秒超时，重试 3 次）。
• 所有异步操作包含 try-catch 并记录日志。
### 5.6 架构自评（模块完成后）
每个模块完成后，Trae 应自动生成一份架构评审报告，包含：
• 模块依赖图（文本描述）
• 是否遵循了接口隔离、依赖注入等原则
• 安全合规检查（是否记录了审计日志、是否过滤了输出等）
• 报告以 Markdown 格式写入 reports/ 目录，随 commit 一起提交。若 reports/ 目录不存在则自动创建，但该目录必须加入 .gitignore（属于临时生成物，不纳入版本管理）。如果创始人希望保存报告，可手动选择保留。
## 六、文档优先级与更新
• 本规则文件（DEVELOPMENT\_RULES.md）是开发行为的权威约束，与 docs/ 下的规范文档互为补充。
• 当本规则与 docs/ 中某文档冲突时，以创始人明确指令为准；无明确指令时，以 **产品方案 → 技术架构 → API文档 → 数据库Schema** 的优先级裁决。
• 规则随项目演进可修改，但修改必须经过创始人同意，并更新版本号与日期。
• **文档更新流程**：本规则文档的修改必须经过创始人批准，并在文末变更日志中记录修改内容、日期和批准人。
## 附录 A：理解确认清单与设计提案示例
以 src/config.py 为例，展示 AI 编程助手在开发前应输出的内容：
### A.1 理解确认清单示例
📋 理解确认清单 — src/config.py
功能列表：
- 基于Pydantic Settings的配置管理类
- 支持从环境变量和YAML文件读取配置
- 敏感字段（API Key）仅从环境变量读取
- 配置优先级：环境变量 > YAML > 代码默认值
依赖模块：
- 无外部模块依赖（基础设施层）
- 依赖第三方库：pydantic-settings, pyyaml
关键参数与配置项：
- DASHSCOPE\_API\_KEY：从环境变量读取，无默认值
- DATABASE\_PATH：默认 data/state.db，可配置
- DRIFT\_THRESHOLD\_PROTECTION：0.25（锁定，见2.3节）
- DRIFT\_THRESHOLD\_REVIEW：0.15（锁定，见2.3节）
风险与边界条件：
- Pydantic Settings禁止extra输入，新增配置必须先修改Settings类
- YAML文件不存在时应使用代码默认值，不抛异常
- 环境变量类型转换（字符串→数字/布尔）需处理
### A.2 设计提案示例
📐 设计提案 — src/config.py
功能拆解：
1. Settings类：继承BaseSettings，定义所有配置字段
2. 配置加载：从.env和config/default.yaml双重加载
3. 配置校验：启动时校验必填项和锁定参数
4. 配置热重载：/config/reload接口支持运行时重新加载
架构设计：
- Settings类使用Pydantic model\_config设置env\_file和yaml\_file
- 通过@cached\_property实现单例模式
- 配置校验器使用Pydantic @field\_validator
技术选型：
- pydantic-settings：类型安全的配置管理
- pyyaml：YAML文件解析
- 选择理由：类型安全、自动环境变量映射、校验器内置
实现步骤：
1. 定义Settings类骨架（所有字段+默认值+类型）
2. 实现YAML加载逻辑
3. 添加字段校验器
4. 实现单例加载函数get\_settings()
5. 编写单元测试
风险点：
- Pydantic V2的model\_config语法与V1不同，需确认版本
- YAML嵌套结构与Settings字段映射需仔细对齐
- 需要创始人确认：是否支持配置的嵌套分组（如llm.max\_tokens）？

